import { MafInputDirective } from './maf-input.directive';

describe('MafInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MafInputDirective();
    expect(directive).toBeTruthy();
  });
});
