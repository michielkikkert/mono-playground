import { CopyMeDirective } from './copy-me.directive';

describe('CopyMeDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyMeDirective();
    expect(directive).toBeTruthy();
  });
});
